from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Build Ml project that predict food delivery time in minutes',
    author='Aniket',
    license='',
)
